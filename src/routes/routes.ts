const routes = async () => {

  switch (key) {
    case '/api/users/:GET':
      break;

    case '/api/users/:POST':
      break;

    case `/api/users/${id}:GET`:
      break;

    case `/api/users/${id}:PUT`:
      break;

    case `/api/users/${id}:DELETE`:
      break;

    default:
      break;
  }
};
